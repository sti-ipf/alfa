= form_for(@room) do |f|
  - if @room.errors.any?
    #error_explanation
      %ul
        - @room.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.label :name, t('room.name')
    %br/
    = f.text_field :name
  .actions
    = f.submit button
